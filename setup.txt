using python 3.11
cd reader_ai/backend

python3 -m venv venv
source venv/bin/activate 

pip install -r requirements.txt

uvicorn app.main:app --reload

front end : npm create vite@latest frontend -- --template react
cd frontend
npm install
npm install